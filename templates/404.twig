{% extends "_layouts/default.twig" %}

{% block _inline_css %}
<style>
    {{source ("404_critical.min.css", ignore_missing = true)}}
</style>
{% endblock %}

{% block content %}

<div class="w-full h-full"> 
     <main class="grid min-h-full place-items-center bg-primary-white px-6 py-24 sm:py-32 lg:px-8">
          <div class="text-center">
               <h1 class="mt-4 type_3xl text-primary-dark">Page not found</h1>
               <p class="mt-4 type_sm text-secondary-dark">Sorry, we couldn’t find the page you’re looking for.</p>
               <div class="mt-12 flex items-center justify-center gap-x-6">
                    <a href="{{siteUrl}}" class="btn btn--accent type_sm font-semibold">Go back home</a>
                    <a href="{{siteUrl}}contact" class="text-sm font-semibold text-secondry-dark">Contact us <span aria-hidden="true">&rarr;</span></a>
               </div>
          </div>
     </main>
</div>

{% endblock %}

{% block pageScript %}
{% endblock %}